# Mditor v3.9.1 全面体检报告

> 审计日期：2026-08-19 · 基线：v3.9.0 · 范围：前端 `src/`、Rust `src-tauri/`、构建产物 `dist/`、依赖清单
> 方法：四阶段并行深挖（安全 / Bug / 性能 / 动效）+ 全部关键发现逐一人工核实 + 机械化检查（npm audit / tsc / eslint / vitest / cargo check）
> 基线状态：tsc 0 错误 · eslint 0 警告 · vitest 149/149 · cargo check 通过

---

## 一、总览

| 阶段 | 🔴 严重 | 🟡 中等 | 🟢 轻微 | 本轮已修 |
|---|---|---|---|---|
| 安全 | 2 | 3 | 2 | 2（S4、S6）+ 2 项已核实无需修 |
| Bug/逻辑 | 2 | 8 | 5 | 6（B1、B2、B3、B5、B9、B10） |
| 性能 | — | 5 | 4 | 1（顺带：append_log 异步化） |
| 动效/体验 | — | 4 | 6 | 1（U1） |

**P0 已全部修复并验证**（tsc/eslint/vitest 150 通过/cargo check 全绿）。剩余项列入路线图。

---

## 二、问题清单

### 第一阶段：安全

| # | 问题 | 位置 | 严重度 | 状态/修复方案 |
|---|---|---|---|---|
| S1 | fs 插件 scope `**`：webview 拥有全盘读写权限（读密钥/写启动项）。前提是先有 XSS，当前净化链完整、无已知注入点，属纵深防御缺失 | `src-tauri/capabilities/default.json` | 🔴 | P1：文档读写改走自带路径校验的自定义 command（canonicalize + 白名单），fs scope 收窄到工作区/assets |
| S2 | CSP `connect-src` 挡不住数据外传：`ai_chat`/`fetch_image` 的 URL 完全由前端传入，Rust 进程代发任意请求，可绕过 webview CSP 外传读到的数据（与 S1 叠加成完整链条） | `src-tauri/src/ai.rs:86-96`、`commands.rs:82` | 🔴 | P1：Rust 侧维护 base_url 白名单（仅允许设置里显式保存过的 host） |
| S3 | assetProtocol scope `**` + `dangerousDisableAssetCspModification`：任意磁盘文件可作子资源加载（含存在性探测） | `tauri.conf.json:29-35` | 🟡 | P1：随 S1 一并收窄到工作区/文档目录 |
| S4 | `append_log` 任意路径写入 + 任意 rename：webview 可控路径，可向 `authorized_keys`/shell profile 追加内容持久化 | `src-tauri/src/commands.rs:31` | 🟡 | ✅ **已修**：强制路径位于 `<app-data>/logs/`，另改 async 移出主线程 |
| S5 | AI API key 明文存储于 `mditor.json`（app-data 目录），同用户进程可读取。已核实：key 不进日志、不进错误信息 | `src/lib/store.ts:80-83` | 🟡 | P2：迁移 OS keychain（`keyring` crate） |
| S6 | sanitize schema 放行 span/mark 的裸 `style` 属性，`position:fixed` 全屏覆盖可伪造 UI 钓鱼 | `src/lib/renderMarkdown.ts:74` | 🟢 | ✅ **已修**：sanitize 后新增裁剪步骤，仅保留 `color`/`background-color`，附回归测试 |
| S7 | 依赖：`image-size`（经 `@turbodocx/html-to-docx`）2 个高危 DoS 通告（GHSA-w3rx-r6r6-pgpr / GHSA-5p2g-fcmc-qvqq），上游无修复版本。触达面仅 docx 导出路径 | `package-lock.json` | 🟡 | P1：跟踪上游；必要时以 overrides 替换或自实现尺寸探测 |
| S8 | 环境项（非 mditor 代码）：`deepseek-free-api/token.json` 含真实 DeepSeek session token 明文落盘 | 工作区根目录 | 🟢 | 建议：轮换该 token 并移出可同步目录 |

**已核实无问题**：`rehypeRaw → rehypeSanitize` 顺序正确；`javascript:` 协议被剥离（有测试锁定）；全仓库唯一 innerHTML 汇入点 MarkdownText 数据全部来自净化管线；AI 回复/批注预览均走同一管线；外部链接协议白名单（http/https/mailto）；`fetch_image` 仅 http(s) 且限 20MB；Milkdown 编辑器内置 `sanitizeLinkHref`；导出 iframe 继承父 CSP；无 `window.open`；无其他 sanitizer 缺口。

### 第二阶段：Bug 与逻辑

| # | 问题 | 位置 | 严重度 | 状态/修复方案 |
|---|---|---|---|---|
| B1 | **预读缓存永不过期**：hover 预读的旧版本被当作最新打开且标记干净，一次保存即覆盖磁盘新内容（数据丢失） | `src/lib/filePrefetch.ts`、`App.tsx:437` | 🔴 | ✅ **已修**：条目记录 size+mtime，`readFresh` 命中先 stat 比对；保存/另存为/外部重载主动失效；删除不校验的 `readCached` |
| B2 | **「全部替换」静默回滚**：`setValue` 不置脏不上抛，替换结果不进自动保存、切标签被旧快照回滚（数据丢失） | `Editor.tsx:550`、`SearchBar.tsx:109` | 🔴 | ✅ **已修**：`setValue` 补 `markDirty` + `onInput`（已核实唯一消费方是替换全部） |
| B3 | 保存窗口期击键被「清脏+回写旧内容」覆盖，关/切标签时永久丢弃 | `src/hooks/useFile.ts` | 🟡 | ✅ **已修**：落盘后重读实时内容条件清脏；另存为对话框期间的编辑同样保住 |
| B4 | AI 流式「停止」只摘前端监听，Rust 侧请求继续下载计费到自然结束 | `src/lib/ai.ts:545`、`ai.rs` | 🟡 | P1：Rust 按 requestId 维护取消令牌 + `ai_stream_cancel` 命令 |
| B5 | `annoMetaCacheRef` 跨文档不清理：A 文档代码行批注元数据注入 B 文档同号批注并持久化 | `Editor.tsx:207` | 🟡 | ✅ **已修**：onLoaded 载入回调清空缓存 |
| B6 | 删除批注的引用剥离无围栏意识，代码块内字面 `[^anno-N]` 示例一并被删 | `src/lib/annotations.ts:332` | 🟡 | P1：剥离循环复用 `findAnnotationRefLine` 的围栏状态机 |
| B7 | 选区上下文横幅读 ref 渲染，「回到全文」点击后不消失 | `AiPanel.tsx:851` | 🟡 | P2：activeSelection 提升为 state |
| B8 | 改动预览「全文模式」应用时不校验文档已变，审查期间编辑被整体覆盖 | `AiPanel.tsx:703` | 🟡 | P1：应用前比对 `getNote()` 与快照，不一致提示重新 diff |
| B9 | `recent` 键损坏时每次打开文件都弹「打开失败」（`list.filter` TypeError） | `src/lib/store.ts:91` | 🟡 | ✅ **已修**：`Array.isArray` 校验回退空表 |
| B10 | 启动加载设置无 catch：损坏存储 → 未处理 rejection + 静默默认值 | `src/hooks/useSettings.ts:88` | 🟡 | ✅ **已修**：catch 保持默认可用并 warn，不自动回存 |
| B11 | 跨文件搜索跳转固定 300ms setTimeout，大文档未开完即跳转静默丢失 | `App.tsx:1450` | 🟢 | P2：等 `openPath` promise 完成后再跳 |
| B12 | 多处 Promise 链无 catch（get_pending_file / file_open / listen / clipboard 等） | `App.tsx` 多处 | 🟢 | P2：统一补 catch + 用户提示 |
| B13 | 导出图片内联不解码 HTML 实体，文件名含 `&` 的本地图片内联必失败 | `src/lib/exporter.ts:76` | 🟢 | P2：src 做 entity 解码 |
| B14 | 设置「应用」持久化失败无反馈 | `SettingsModal.tsx:134` | 🟢 | P2：catch 显示失败原因保持弹窗 |
| B15 | 自定义 CSS 快速换路径存在完成序竞争 | `useSettings.ts:83` | 🟢 | P2：序号守卫丢弃过期结果 |

**已核实无问题**：定时器/监听器/Observer/编辑器实例清理成对完整；文件切换 token 防竞态；`aiThread.ts`/`activity.ts`（新文件）无问题。

### 第三阶段：性能

| # | 问题 | 位置 | 影响 | 状态/修复方案 |
|---|---|---|---|---|
| P1 | 渲染管线（unified+KaTeX+lowlight ≈500KB）静态打进首屏主 bundle，注释声称懒加载与实现不符 | `MarkdownText.tsx:24` → `renderMarkdown.ts:21` | 主 bundle 924KB，冷启动多解析 ~500KB | **P1 首选**：拆 `renderCache.ts`（同步探针）与 `renderPipeline.ts`（插件链），首次 miss 时动态 import，idle 预热 |
| P2 | 工作区搜索 = 最多 500 次串行 readTextFile IPC，无内容缓存 | `workspaceSearch.ts:167` | 500 文件查询 1-3s | P1：Rust 命令一次完成 walk+read+match；保守方案 mtime 缓存 + 并发池 |
| P3 | rich 模式每 keystroke 全文序列化（Milkdown markdownUpdated 固有） | `useMilkdown.ts:517` | 1MB 文档打字卡顿主因 | P1：onInput 上游 150ms debounce 后取一次 `getMarkdown()` |
| P4 | 内联箭头函数击穿子组件 memo；SettingsModal 未包 memo | `App.tsx:1549` 等 | 打字期间整树 60Hz reconcile | P1：~8 处箭头提升 useCallback + SettingsModal 包 memo |
| P5 | PNG 导出 dataURL 字符串链 + 120MP 画布，瞬时峰值可超 1GB 触发整页 reload | `exporter.ts:270` | 大文档 2x 导出可能丢工作现场 | P1：改 `domToBlob` + 像素预算收到 ~64MP |
| P6 | docCache 预算按源字符计，实际驻留 JSON 放大 3-10 倍（满预算多驻留 25-80MB） | `docCache.ts:23` | 多大文档标签堆基线抬高 | P2：按 `JSON.stringify` 长度计费或降预算 |
| P7 | FileTree hover 预读无意图延迟（扫过 30 行 = 60 次 IPC） | `FileTree.tsx:924` | 慢盘 IO 排队 | P2：hover 150-200ms 确认后预读 + 并发=1 |
| P8 | sv 模式打字停顿后三套独立全文扫描（大纲/批注/锚点摘录） | `Outline.tsx:42` 等 | 大文档每次停顿 2-3 次 O(doc) | P2：单 pass 共享或移入 parseWorker |
| P9 | FileTree 无虚拟化（react-virtual 仅用于 AiPanel） | `FileTree.tsx:806` | 数千节点全展开掉帧 | P2：扁平化 + useVirtualizer |
| P10 | vendor-milkdown 2.72MB 单 chunk 全 eager（内含 KaTeX） | `vite.config.ts:49` | 冷启动多解析 ~270KB | P2：katex 拆独立 chunk 按需加载 |
| P11 | `append_log` 同步命令在主线程做文件 IO | `commands.rs:31` | 打字敏感时刻可能撞上阻塞 | ✅ **已修**（随 S4 改 async） |
| P12 | AiPanel 流式期间 rows 派生树每 rAF 重建 | `AiPanel.tsx:337` | 长会话流式每帧小 O(100) 重算 | P3：流式只改末条 |
| P13 | watcher 全等比较大字符串无 length 短路 | `useFileWatcher.ts:81` | 大文档外部改动一次 O(n) 比较 | P3：先比 length |

**关键指标路径（本地桌面应用，对应冷启动/TTI/LCP）**：P1+P10 落地后首屏 JS 从 ~3.8MB 降到 ~3.0MB（冷启动 parse/compile 时间 -25% 左右）；P3+P4 决定大文档打字 TTI 体感；P2 决定搜索场景可用性。

### 第四阶段：动效与体验

| # | 问题 | 位置 | 影响 | 状态/修复方案 |
|---|---|---|---|---|
| U1 | 导出全程无进行中反馈，大文档导出界面「假死」可重复点击 | `App.tsx:855` | 用户重复触发重任务 | ✅ **已修**：状态栏「正在导出…/导出完成/导出失败」 |
| U2 | 可点击行（标签/大纲/最近/文件树）全是无 tabIndex 的 div，键盘不可达 | `TabsBar.tsx:31` 等 | 键盘用户无法操作 | P1：`tabIndex={0}` + Enter/Space + focus-visible |
| U3 | 批注弹窗 resize 处理器未 rAF 合并，拖拽窗口连续强制布局 | `AnnotationPopover.tsx:405` | 拖拽卡顿 | P1：走 rAF 合并（滚动路径已有） |
| U4 | 侧栏开合 transition width 0.28s，每帧整页 reflow | `global.css:292` | 低配机开关掉帧 | P2：宽度瞬变，只过渡 opacity/translateX |
| U5 | 弹窗/菜单只有入场动画，关闭瞬间卸载，动效不对称 | `global.css:3232` 等 | 观感廉价 | P2：reverse + 延迟卸载 |
| U6 | 分隔条缺 `touch-action:none`，触屏拖动被判滚动手势 | `global.css:335` | 触屏无法调节宽度 | P1（一行） |
| U7 | `will-change:width` 不可合成属性白建合成层 | `global.css:331/1709` | 误导 + 层内存 | P2：删除 |
| U8 | 设置折叠区 max-height:1000px 过渡，节奏失真 | `global.css:3261` | 动画截断 | P3：grid-rows 0fr→1fr |
| U9 | 主按钮 hover 用 `filter:brightness`（绘制属性，10 处） | `global.css:1388` 等 | 重绘 | P3：`--accent-hover` + background-color |
| U10 | focus-visible 仅 4 处，多数按钮依赖 UA 默认焦点环 | `global.css` | 深色主题观感不一致 | P2：全局 focus-visible 规则 |
| U11 | 主滚动区全部系统默认粗滚动条 | `global.css:3640` | 观感割裂 | P2：全局 thin scrollbar |
| U12 | smoothJump 的 `{once:true}` wheel 监听不滚轮永不移除 | `App.tsx:1196` | 轻微监听堆积 | P3：setTimeout 清理时一并移除 |
| U13 | 按钮 :active 缩放 transition 只在按下方向生效 | `global.css:39` | 松手生硬 | P3：transition 移到基础规则 |

**已核实良好**：reduced-motion 三层兜底；高频事件普遍 rAF 合并/debounce；切文件 loading bar + 大文档遮罩；AI 面板/搜索的 loading、空态、错误态齐全；图片懒加载 + aspect-ratio 预留；z-index 梯度无冲突。

---

## 三、修复路线图

### P0（本轮已全部完成 ✅）
B1、B2、B3（数据丢失三连）· B5、B9、B10（健壮性）· S4、S6（安全加固）· U1（导出反馈）

### P1（本周建议）
1. **P1 渲染管线懒加载**（首屏 -~500KB，半天）+ P10 katex 拆包
2. **P3 序列化节流**（大文档打字体感）+ **P4 memo 补齐**（1 小时）
3. **B4 AI 流式后端取消**（Rust 取消令牌，计费相关）
4. **S1/S2/S3 权限收窄**（文档读写自定义 command + base_url 白名单 + asset scope）
5. **P2 工作区搜索 Rust 化**（500 IPC → 1）
6. U2 键盘可达 + U3 resize rAF + U6 touch-action（合计 1 小时）
7. S7 跟踪 image-size 上游 / B6 批注剥离围栏感知 / B8 全文应用校验 / P5 PNG 导出内存

### P2（下月）
S5 keychain 迁移 · P6 docCache 计费 · P7 预读节流 · P8 单 pass 扫描 · P9 FileTree 虚拟化 · U4/U5/U7/U10/U11/U12 · B7/B11/B12/B13/B14/B15 · P12/P13

---

## 四、验证记录（v3.9.1）

| 检查 | 结果 |
|---|---|
| `npx tsc --noEmit` | 0 错误 |
| `npm run lint`（eslint 10） | 0 错误 0 警告 |
| `npm test`（vitest） | 14 文件 150/150 通过（新增 style 白名单回归 1 例） |
| `cargo check` | 通过（append_log 改造后） |
| `npm audit --registry=npmjs.org` | 仅 image-size 2 高危（上游无修复，见 S7） |
