# Mditor 3.5.0 全面优化实施计划

> **For agentic workers:** 本计划在当前会话内直接执行（自主模式）。规格来源：用户确认的 60 项审计总表（P0/P1/P2）。

**Goal:** 落地 60 项审计优化（功能正确性 / 性能 / 安全 / 工程），版本提升至 3.5.0，产出 NSIS 安装包。

**Architecture:** Tauri 2 (Rust) + React 18 + Milkdown/Crepe + Vite。前端在 `src/`，Rust 在 `src-tauri/src/`。

**Tech Stack:** TypeScript / React / ProseMirror(Milkdown) / Rust(reqwest) / Vite / Vitest / NSIS

---

## 状态校准（与总表的差异）

- #42 泄漏探针：**已完成**（main.tsx 无引用，文件已删）。剩余动作：删除仓库根的 `HeapSnapshot-*.json` 调试残留。
- #37 sanitize：**已实现**（renderMarkdown.ts 已接 rehype-sanitize，含测试）。降级为验证 MarkdownText 全部 innerHTML 路径走消毒管线。
- 当前版本 3.4.2（非 3.4.0）→ 目标 3.5.0。
- `@tanstack/react-virtual` 已在依赖中（仅 AiPanel 使用）→ FileTree 虚拟化可直接复用。
- #57 execCommand/window.find：WebView2(Chromium) 下均可用且现有代码已有回退结构，**保留现状**（报告注明）。
- #53 crate-type 保留（模板标准三件套，未来移动端需要）。

## 任务分解

### Task A（子代理 Rust）：`src-tauri/src/ai.rs` + `commands.rs`
- #30 静态共享 reqwest Client（`OnceLock`，connect_timeout 10s；流式请求不带总超时；fetch_image 用每请求 `.timeout(30s)`）
- #30 SSE 缓冲区 cursor 式解析 + 定期压缩，消除 O(n²) drain
- #30/#199 前端通道 send 失败（drop）即中止拉流
- #316 移除 120s 总超时对长流的掐断
- #41 错误消息中的上游响应体截断（≤300 字符）
- 验证：`cargo check`

### Task B（子代理 杂项 hooks/lib）
- `useMemoryGuard.ts` #5：recheck 定时器可清理 + cancelled
- `useSettings.ts` #6：函数式 setState（支持 updater）
- `SettingsModal.tsx` #7：草稿仅在打开时同步；#58 测试连接不持久化草稿
- `outline.ts` #9：slugCache 超限逐条淘汰最旧而非整体 clear
- `imageManager.ts` #4 唯一文件名（UUID/计数器兜底）；#34 缓存 appDataDir + 单次 createDir
- `useResizable.ts` #36：pointercancel 清理
- `exporter.ts` #32 PNG 导出 bigDoc 门控/降 scale；#60 定时器句柄清理
- 验证：`npx eslint <files>` + `npx vitest run`

### Task C（子代理 面板组件）
- `SearchBar.tsx` #17：输入防抖（~200ms）
- `SelectionToolbar.tsx` #23：document 级监听 + closest 判定（重建后仍有效）
- `AiPanel.tsx` #24：仅当接近底部才自动滚动
- `useAnnotationMarkers.ts` #22：无注解时缓存早退
- `MenuBar.tsx` #35：entries 记忆化（动态项才重建）
- `MarkdownText.tsx`/`renderMarkdown.ts` #37 验证消毒覆盖；#29 LRU 增加字节上限（如 8MB）
- 验证：`npx eslint <files>` + `npx vitest run src/lib/renderMarkdown.test.ts`

### Task D（主会话）：编辑器核心热路径
- `Editor.tsx` #10 React.memo；#20 isSaving 以写入完成为准复位；#28 useImperativeHandle 依赖稳定化
- `useMilkdown.ts` #11 heading 打标事务 meta 跳过 + changedRanges 增量；#12 加载避免双解析；#13 sv getHTML 签名缓存；#18 模式切换空转跳过；#19 sv onInput 防抖派生重算；#50 span 颜色正则与 remarkTextColor 共享
- `App.tsx` #15 单遍 word count（抽 `lib/textStats.ts` + 测试）；#33 已 dirty 早退；#45 死 effect；#46/#47 keydown 统一走表驱动 dispatchMenu；#48 适度拆分（菜单动作 hook）；updater 移除的接线
- `FileTree.tsx` #2 renaming 精确匹配；#3 大小写不敏感 + 分隔符边界；#16 refreshAll 并行 + cancelled；#25 多选 Set 拷贝消除；#26 目录内容未变复用引用；#27 大目录虚拟滚动（复用 @tanstack/react-virtual）
- `store.ts`/`useFile.ts` #14 pushRecent 已在首位跳过全量写；#8 建议文件名清洗 Windows 非法字符
- `useFileWatcher.ts` #21 外部变更比较基于 lastSaved 文本而非全量 getMarkdown 序列化

### Task E（主会话）：配置 / 安全 / 工程
- `capabilities/default.json` #1 补 fs:allow-watch(-recursive)/unwatch；#38 删 pandoc 死权限；移除 updater:default/process:default
- `tauri.conf.json` #39 删 unsafe-eval；#43 删 plugins.updater；#56 targets→nsis
- `index.html` #55 meta CSP 补 font-src 同步删 unsafe-eval
- `vite.config.ts` #51 minify 改 esbuild（drop console）
- `tsconfig.json` #52 noUnusedLocals/noUnusedParameters → true（修掉暴露的死代码）
- `lib.rs`/`Cargo.toml` 移除 updater/process 插件与依赖；tokio 直接依赖若无用移除；删除 CHECK_UPDATE 菜单
- `release.yml` #54 加 rust-cache；去 updater 签名/latest.json 流程
- #44 README 重写；新增 CHANGELOG 3.5.0
- 版本 3.5.0：package.json / package-lock / tauri.conf.json / Cargo.toml(+lock)
- 删除根目录 `HeapSnapshot-*.json`（未跟踪调试残留）

### Task F：集成验证与打包
- `npm run build`（tsc + vite）、`npm run lint`、`npm test` 全绿
- `cargo check` 通过
- `npm run tauri build` → NSIS 安装包；报告产物路径与变更清单
- 显式不做：git commit（未经指示）；#57 保留现状；#53 crate-type 保留

## 验收标准

1. 全部 P0/P1 项落地或有书面豁免理由；P2 尽数落地
2. tsc / eslint / vitest / vite build / cargo check 全绿
3. 产出 `src-tauri/target/release/bundle/nsis/Mditor_3.5.0_x64-setup.exe`
4. README/CHANGELOG 记录 3.5.0 变更
