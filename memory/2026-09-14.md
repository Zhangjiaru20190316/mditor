# 2026-09-14

## 鸿蒙内测包上传 v4.12.3 Release（08:00–08:15）

用户问「鸿蒙版本没有上传吗」→ 核实：v4.12.3 Release 只有 Windows NSIS；release.yml 无鸿蒙构建线；本地已签名 HAP 停在 09-13 12:23 白屏修复版（不含 v4.12 云同步与 4.12.1–4.12.3 修复，AppScope 版本号还是 4.10.0）；signing/ 整目录被 harmony/.gitignore 忽略（正确）。关键限制：**debug 签名 HAP 只能装 Profile UDID 白名单设备**，公众分发需 AGC 发布证书/市场上架。用户选「重构建+签名，作为内测包上传」。

### 执行

- AppScope/app.json5：4.10.0/1000000 → **4.12.3/1001203**（commit feb97df，已推）。
- `npm run build:harmony`（scripts/build-harmony.mjs：vite --base ./ → rawfile/web → ohpm → hvigorw assembleHap）→ 未签名 HAP 7768KB（hvigor 输出含 S3Bridge 编译，确认新代码打进）。
- `node scripts/sign-and-install.mjs` 签名成功 7806KB（profile type: debug）；hdc 因无设备连接跳过（脚本报成功文案但实际未装，别被最后一句骗）。
- `gh release upload v4.12.3 --clobber` 上传资产 **Mditor_4.12.3_harmony-debug-signed.hap**；`gh release edit --notes-file` 写明两个资产用途+调试签名限制。

### 网络故障与重试（新形态）

- clash 7890 进程挂了（netstat 无监听）→ git（读 git config 代理）全挂；同时直连 github.com 被 GFW reset，**两条路都不稳**。
- 教训 1：`git push | tail` 管道吞掉 git 退出码，`|| (retry)` 永不触发——判断推送成功要 `grep "main -> main"`。
- 解法：后台循环 30 次×20s，直连/代理交替（`git -c http.proxy= ` 绕过 vs 显式 7890），第 11 次直连成功。与 08-22 网络中断同款，后台重试是对的路子。

### 并行会话共存（重要）

- 推送时发现本地多出非我做的提交：另一会话在跑鸿蒙流水线 **P1–P4**（07:49–08:11：AI 四命令 ArkTS SSE 代理→AI/富导出前端翻转→S3 六命令 SigV4 桥+云同步鸿蒙装配→WatchManager 外部监听），持续出提交，我推送时顺带把它们带到远程（同向工作，无冲突）。
- **HAP 构建时点在 P2 后、P3/P4 前**：内测包缺鸿蒙侧云同步装配与外部监听；Registry.ets 卷入 07:54 未提交微调（编译通过，风险低）。已在 Release 说明标注「构建之后合入的功能在下一个内测包」。
- 取证方法：判断构建是否卷入未提交代码 → 对比文件 mtime 与构建时间窗（vite 阶段读 src/，hvigor 阶段读 ets/）。
- 教训：同仓并行会话时，push 前先 `git log origin/main..HEAD` 看清会带上去什么；别追着移动目标反复重构建 HAP，等该会话阶段跑完再出包。

### 遗留

- 另一会话仍在工作（EntryAbility.ets/FileManager.ets 未提交改动中）；其阶段完成后应重构建+重签名 HAP 并 --clobber 重传，补上 P3/P4。
- 公众鸿蒙分发阻塞在 AGC 发布证书（之前代操作拿的是调试证书）。

## 鸿蒙功能补全 P0–P7 全量完成（07:30–09:00，即上节「另一会话」本尊）

按工作区根 `prompt-harmony-completion.md`（一次性开发提示词）跑完全部七阶段。**P0–P4 的提交即上节记录的那批**；本节补记 P5–P7 与全局结论。

### 各阶段落点（9 个 commit，d807584…6760adb）

- P1/P2 前端翻转：HARMONY_CAPS.ai/richExport=true + caps.test.ts 快照（4→8 用例，防回退锚）。PDF 保守 false（iframe print 待真机 spike）。
- P2 桥侧 AiBridge.ets（约 700 行）：SSE 用 `requestInStream`+`on('dataReceive')`，**该 API resolve 值就是状态码（Promise<number>，非 HttpResponse）**；TextDecoder `{stream:true}` 增量解码防多字节截断；取消=登记表+主动 destroy（空闲流也能立即停）。
- P3 桥侧 S3Bridge.ets：手写 SigV4。**坑与解法**（高复用价值）：
  1. ArkTS 禁内联对象字面量类型 `{k,v}` → 提前 `interface StrPair`；
  2. `@ohos.xml` API 22 形状：构造只收 ArrayBuffer、无 setCallback，事件级回调是 `parse({tokenValueCallbackFunction:(eventType,ParseInfo)=>bool})`，ParseInfo 用 getName()/getText() 方法；
  3. `@ohos.url.URL` 无 `scheme` 属性（用 protocol 去尾冒号；hostname/port 分开取，port 默认端口时为空串）；
  4. **SigV4 向量核验不能靠模型记忆的「官方向量值」（我记的两个全错）**——真 oracle 是 `@smithy/signature-v4`（AWS SDK v3 在用签名器，devDependency），固定 signingDate 确定性比对，5 场景全过（中文/空格/+ key、List 查询集、PUT+meta、token、非默认端口）。已固化 `scripts/sigv4-check.mjs`（20 断言）；
  5. 自查发现并修复：s3_get/list/test_connection 漏带 x-amz-security-token（STS 下会被拒）——object_store 是全请求附带。
  6. etag 去引号 + lastModified 统一 ISO：engine 的 isContentEtag 期望裸 32-hex、list/put-head 两路径 lastModified 必须同格式否则 manifest 误判远端变更。
- P3 前端：isSyncSupported 改 `!== "browser"`（browser 预览变 false）；s3Get harmony 分支 {base64}→fromBase64（harmony/index.ts 导出复用，D4）。
- P4 WatchManager.ets：**有意偏离提示词的「目录 mtime 预筛」**——外部编辑已有文件只改文件自身 mtime，目录预筛漏主场景，改全量 stat 快照 diff（5000 上限/2s/5s/引用计数/重载全清）；删除事件发 kind:'any'（vaultIndex 过滤表不认 'delete'，plugin-fs 对象型 kind 本就退化为 'any'）。
- P5：trashFile→/AppData/trash/<epochMs>-<原名>（跨边界 rename 失败降级复制+删原件；EntryAbility 启动清理 30 天）；FileTree 文案 bug 修复走 fileOps.deleteConfirmLine（能力×运行时三分支），agent/tools.ts 两处硬编码同步动态化。
- P6：无设备（hdc list targets 空）→ 按提示词纪律 spike 前置不可执行，multiWindow 保持 false，双路线计划写入 README。
- P7：版本四处 4.13.0（app.json5 实际是 1001203/4.12.3 而非提示词说的 4.10.0——按实际代码推 scheme 100+MM+PP → 1001300）+ CHANGELOG + README 能力矩阵/真机清单重写。

### 门禁与产物

vitest 742→**758** 全绿；tsc/vite/eslint 绿；`hvigorw assembleHap` 绿；`node scripts/sigv4-check.mjs` 20/20；`npm run build:harmony` 完整链路出 **7785KB 未签名 hap**（含全部 P1–P5 代码——上节遗留的「重出内测包补 P3/P4」现在可以直接 `node scripts/sign-and-install.mjs` + `gh release upload` 出 4.13.0 内测包）。

### 教训沉淀

- **Bash heredoc 折叠反斜杠的坑再现**：python heredoc 里写 `\n` 匹配 TS 源码失败——含反斜杠的精确串匹配一律用 Edit 工具或 chr(92) 构造（09-13 老教训，今天又踩）。
- 提示词「已核实」事实要复核：app.json5 版本描述与实际不符（4.10.0 vs 4.12.3）；FsWatchEvent kind 值消费方（useFileWatcher vs vaultIndex）接受集不同。
- 同仓三会话并行：我只提交自己路径下的文件（Rust 的 v4.12.4 崩溃取证 WIP 是另一条线，lib.rs/commands.rs/s3.rs 的未提交改动不碰不提）；那个 WIP 叠在我的 4.13.0 版本号之上，合并时注意。

## 云同步「打开即崩溃」：根因实锤 + 全套修复 + 三轮真机验证 ✅（08:00–09:20，即上节「另一条线」本尊）

用户报修：启用云同步后软件崩溃。两探索 agent + 亲测定位，已完成修复（8 文件，未提交时叠在 4.13.0 之上）。

### 诊断实锤（Windows 事件日志）
- Application 日志 4 次 `mditor.exe` 崩溃（0xc0000409 = CRT abort，P9=7），09-13 17:31–18:31，版本 4.12.0/4.12.1。
- 时序 = 「启用 → syncOnStart 默认 15s 自动首同步 → 崩 → 重启再崩」循环；崩溃后 enabled 已持久化，重启必复现。
- 无痕原因：release `panic="abort"` + Rust 零 panic hook/日志，minidump 已被系统清理。

### 根因与修复
1. **base64 IPC 内存风暴**（崩溃元凶）：s3Put 把 ≤50MB 文件 btoa 后走单次 JSON IPC（~4.3x 内存放大 × 并发 3 × 4 根；E:\笔记 嵌套包含 cmc/作文两个根，同批文件传 3 遍）。→ **Rust 文件通道**：s3.rs 新增 `s3_upload_file`/`s3_download_file`（.mditor-tmp 先写后改名），commands.rs 新增 `local_files_equal`（256KB 流式比对）/`local_copy_file`；SyncIO 改路径通道（s3PutFile/s3GetFile），字节全程不经 JS；并发 3→2。旧 s3_put+base64_decode 删除（鸿蒙桥自有语义，不受影响）。
2. **取证基建**：lib.rs `install_panic_logger()`——panic hook 追加写 `<appdata>/logs/panic.log`（abort 下 hook 仍执行；PANIC_LOG_PATH OnceLock 在 setup() 回填，之前兜底系统临时目录）。
3. **必现配置 bug**：用户 endpoint 是未替换模板 `https://oss-{region}.aliyuncs.com`；SK 尾随空格且 syncConfigPayload 唯独不 trim 它 → SigV4 必败。→ SettingsModal applyAll/runSyncTest 拦截 `{}` 占位符；SK/token trim。
4. 鸿蒙分支保留 base64 通道（ArkTS S3Bridge 语义不变）。

### 验证（全绿）
- cargo test 18 ✓；vitest **761** ✓（其他会话 758 + 我 3 个新用例）；eslint/tsc 干净。
- 本地 s3rver 三轮真机验证（dev 构建；测试工作区 = a.md + 10MB big.bin + nested + .hidden）：
  首轮 `up:3 failed:0`（10MB 完整上传、点开头忽略 ✓、manifest 落盘 ✓、无 panic ✓）→ 变更轮 `up:1`（只传改动的 a.md，远端确认 v2）→ 收敛轮 `up:0 down:0 failed:0`。修复后 run-fail 零新增。

### 测试环境教训（高复用价值）
- **MinIO 社区版已归档**：dl.min.io 一律 410 Gone。→ `s3rver`（npm）可平替。
- **s3rver 内置账号是 `S3RVER`/`S3RVER`（全大写）**——不是 minioadmin 也不是 `S3rver`（displayName 大小写不同）。AK 不匹配 → InvalidAccessKeyId 403。
- s3rver SigV4 未实现（v4 账号匹配即过）；`--allow-mismatched-signatures` 只豁免签名不匹配，不豁免 InvalidAccessKeyId。
- 排障利器：devMode（`settings.devMode=true`）下 sync:run-fail/sync:done 全量落 dev-events.log（含 up/down/failed 计数）；靠心跳 uptime 识别「当前是哪个进程实例」，别信时间感。
- 长链路后台进程（npm→node→cargo run→mditor.exe）taskkill 父进程后子进程可能残留占端口/占 cwd（TaskStop 后 netstat 复查）。

### 遗留 / 提醒用户
- 嵌套工作区根会让同批文件按多个 prefix 重复上传（本次不改同步语义，建议后续加「跳过被包含的根」）。
- 用户重新启用云同步前需填真实 endpoint（`{region}` 会被保存拦截）+ 重贴 SK；修复版发装后首同步才真正开始。
- panic.log 的 ts 是 epoch ms（std 无日期格式化，未为它引依赖）。
- 我的 Cargo.toml（tokio fs 依赖）被并行会话的 6760adb 顺带卷入提交——新命令依赖它，归属正好，无需再动。
