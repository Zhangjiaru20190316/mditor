# 2026-08-27

## mditor v4.5.0 发版 + 官网动效美术升级（已完成并推送）

用户需求：推送最新版 + 优化官网动效与表现力/美术效果。AskUserQuestion 三连确认：
推 main + 打 tag v4.5.0（完整发版）；按主题拆三个提交；官网美术跟随 promo-v4.5.0 炫光风格。

### 官网改造（site/，纯静态零依赖）
- 版本内容 4.4.0→4.5.0：下载链接/hero 徽标/演示文案×3/根 README；What's New
  置顶全宽亮点卡（渐变描边 border-box 技法 + 深色 kbd-chip + 特性 pill）。
- 美术提炼自 promo-v4.5.0.html：hero 三层光晕漂移（transform 循环 21/26/31s）、
  漂浮 Markdown 符号、SVG feTurbulence 噪点（data URI，opacity .05）、
  h1/h2/亮点卡标题 background-clip:text 渐变（@supports 守卫）、logo 光环呼吸。
- 动效：hero 子元素 rise-in 编排（nth-child 延迟）、reveal 兄弟 stagger
  （JS 按父容器分组 70ms 步进封顶 320ms）、卡片/wn-item 鼠标聚光（--mx/--my）、
  截图 3D 轻倾斜（rAF 节流 ±2.2deg）、CTA 流光 sheen（双 keyframes 强制重放）、
  顶部滚动进度条（并入 to-top 监听）、导航 active 下划线、kbd 立体键帽、
  lightbox 缩放入场。红线：只动 transform/opacity；reduced-motion 全局
  `animation/transition: none !important` + JS matchMedia 跳过聚光/倾斜。

### 提交与发版
- 41b4d34 feat(ai) / e3880c9 perf(editor) / 9792d76 docs(site)；测试文件归属
  按关键词判定（ai.test.ts→feat，types.test.ts→perf）。门禁 331 tests/tsc/build 全绿。
- 推 main 后打 tag v4.5.0 触发 release.yml（Windows NSIS，冷构建 10-20min）。

### 教训（IAB 浏览器实测）
- **首帧截图会撞 CSS 入场动画的 backwards fill**：动画时钟被环境冻结时
  （后台 tab/首帧 capture），`fill-mode: backwards` 让 hero 整块 opacity:0，
  截图像素空白但 DOM 正常。reload 后前台播放即正常——真实用户无感知，
  但今后做「内容默认隐藏」型入场要意识到这个坑。
- **IAB guest 合成器会丢文档层**：反复 hash 导航后整页只画 fixed 层（导航/
  进度条），DOM 诊断（reveal 计数、.has-js、#demo-input 已填）全部正常。
  判定方法：locator 读 DOM 状态 vs 像素截图对不上 → 环境渲染 bug，换新 tab
  即恢复。cua.scroll 大步长会 30s 超时（平滑滚动冲突），用 End/Home 键代替。

## mditor 大文档卡顿治理（v4.6.0 性能篇，已完成）

任务：消日志里 152KB/3447 行 KaTeX 文档的 MD-1003（1.2~1.5s × 24）与
MD-9001（单窗 77~103 掉帧）。全程测量驱动，产出 perf/ 基建 + 两个根修 + 评估。

### 方法论（可复用）
- 真实日志先读：dev-anomalies.log 聚合（MD-1003×24 / MD-9001×52 / MD-1011
  全量替换×17 / MD-1002 位移×53）→ 时间线对齐 dev-events.log → 问题档案。
- 测量：`tauri dev --config tauri.dev.conf.json`（独立 identifier
  com.mditor.app.dev，避开单实例转发+不碰用户状态）+ WebView2
  `--remote-debugging-port` + 自写零依赖 CDP（Node 原生 WebSocket）。
- 坑：tauri.dev.conf.json 不会被 tauri dev 自动合并（exe 里无 .dev 标识），
  必须 `--config` 显式传；seed mditor.json 时 JSON 反斜杠会被吃（用正斜杠）；
  测试打字会经 autosave 写文档——必须用副本（perf/fixtures，gitignore）。

### 两个根修（均已单独提交）
1. **聚焦切换全文档样式重算（主犯）**：crepe 只在 .ProseMirror-focused 定义
   `--prosemirror-virtual-cursor-color` → 失焦/回焦时该变量在「无→有」翻转 →
   Blink 作废全部后代样式。blur→focus() 实测 762ms（用户侧=点工具栏再点回
   正文，正是 MD-1003 的反复来源）。修复=global.css 让变量恒定存在（值不变
   → no-op）→ 0ms。**教训：CSS 自定义属性挂在状态类上=全子树样式重算雷。**
2. **虚拟光标每事务强制布局读**：prosemirror-virtual-cursor 每事务/
   selectionchange/ResizeObserver 同步读 DOM 选区 rect（脏布局=全文档同步
   布局，773ms/3clicks）。patch-package：rAF 合并+(doc,head,width) 同位跳过。

### 数据（221KB 同型文档，七场景基准）
- 点击×10：max 945→167ms，>200ms 长任务 1→0，事件 p95 976→192ms（p50 24 不变）
- 双击+sel-btn 加粗：0 长任务（用户日志原 1.2~1.5s）
- 打开仍是 ~2s 长任务：实测与解析无关（221KB 解析 <5ms），100% 是首次全量
  布局（PM updateStateInner 的 focusNode 读触发）；c-v 是唯一杠杆。
- big 档对照：打开 3411→1291ms、打字 p95 144→32ms、滚动 p95 65→6ms。
- **阈值结论**：对 ≤221KB 文档解析可忽略，降 docCache 阈值/解耦 worker 预解析
  无可测收益（本轮不做）；big 档默认保持关（铁律），但值得在文档里推荐
  3000 行+用户开启。

### 走过的弯路（别再走）
- scrollDebug tick 每帧双读哨兵 rect（A/B 实验：停用后 1939→1853ms，非主犯）。
- floating-ui TooltipProvider observeMove 轮询（profile 归因 750ms）：打补丁
  实测无差异（1268→1262ms）——它只是替同帧绘制预付布局款，**不是寄生成本**，已回退。
- 打字 ~140ms 延迟是 dev 构建+CDP 合成输入的固定开销（用户生产日志
  worstInputLagMs=6ms），非真问题。
- 160+ 表格/列表拖拽句柄 TooltipProvider 常驻 DOM（问题文档表格多），确认隐藏态无轮询成本。

### 交付
- 提交：e8650c5(v4.6.0 数学，遗留完成态先隔离) → 8ab3b06(虚拟光标 patch) →
  e2384ef(focus CSS 熔断) → 3a5a765(perf 基建+docs)。
- docs/performance.md 新章节「交互路径卡顿治理」；CHANGELOG 4.6.0 增性能节。
- 打包 v4.6.0 NSIS。

## mditor 大文档卡顿第二轮：选中与编辑路径（v4.6.1，已完成）

任务：消 MD-1003 复发（点公式→点正文→点工具栏 2-3.8s）+ MD-4011
「DOM 增长 14.5 分钟 +213,857」。

### 归因（先证伪）
- **MD-4011 = 检测器假阳性**：心跳三计数（dom/cm/katex）全程平稳；真因 =
  切空文档(267) → 切回大文档(214,278)，趋势窗口把切换当增长。修 =
  HeartbeatPoint.docKey + sameDocTail 尾段切割。
- **选中链路真凶 = 全量渲染 DOM（21 万节点）的原生强制布局**：CPU 剖面
  gBCR 724-1470ms + (program) 巨块，JS 占比 <10%。scrollDebug 哨兵每帧
  双读是最大读者但属预付布局款（杀读者无用）。**唯一杠杆 = c-v**。
- 拆解坑：@codemirror/view 的 hasFocus 在 6.43.8 是 **getter**（不是方法，
  别看到无括号就判 bug）。

### 主杠杆：bigDocViewport 子开关（v4.6.1 新设置，默认关）
- c-v（data-big + cvMemory）与减配（CodeMirror/Latex 关）解耦：
  sizeIsBigDoc / isBigDoc / isBigDocCv 三层（memory.ts）。
- 实测：拖选 816~1832→40ms、三击 ~1500→64~96ms、点公式 ~1000→24~32ms，
  公式照常渲染。Ctrl+A 跨视口 PASS。
- **踩坑**：maybeRecreateForBigDoc 只判减配档——仅 cv 档翻转时调用方已
  return 而它空转 → 空编辑器（静默）。档位判定必须覆盖全部档位。

### 其他已落地
- 停用 crepe Toolbar（先补齐链接/行内公式按钮到自研工具栏再禁）。
- SelectionToolbar frame/commit 拆分（拖选零序列化，提交走 PM
  textBetween）+ StatusBar 尾随防抖。
- crepe 行内公式提示 patch：Schema 单例+destroy 旧 view+同公式复用（泄漏根修）。
- code-block teardown 聚焦/选中时重排（卫生）；plugin-listener 停键
  eq+serialize（225KB 实测 80ms）挪 requestIdleCallback + 可开关打点。
- 评估不做：C2/C3（剖面占比 <10%）、B4（74ms/11 键，不值当动已验证补丁）。

### 基准纪律（血泪升级）
- **同一开发机跨时段整机负载漂移 2-3 倍**：同代码 HEAD 两次跑出 728ms 与
  1832ms。单轮跨时段对比=废纸。必须同窗口 ABAB 交错（select-bench.mjs
  累积轮次）+ 剖面归因。
- Tauri store 的 settings 在 Rust 侧缓存：手改 mditor.json 后 reload 无效，
  必须整个重启 dev 实例。
- TaskStop 杀 tauri dev 后 vite/cargo/mditor.exe 子进程残留占 1420/9223，
  netstat 找 PID 补刀 + tasklist 区分生产实例（14540）。
- CDP 抓 rect：gBCR 是数据属性（包方法），offsetWidth 在
  HTMLElement.prototype。
