# 2026-08-26

## mditor v4.5.0 — AI 一键修复 Markdown 格式（已完成，未提交）

用户需求：AI 功能增加一键修复文档 MD 格式。AskUserQuestion 未获回答，按推荐项实施：
自动打开改动预览（非直接替换）+ 双入口 + 严格只修语法。

### 实现（7 文件，全部验证通过：331 tests / tsc / vite build）
- `src/lib/ai.ts`：新增 `buildFormatFixMessages(note)` —— 全文原文不截断（区别于
  buildSystemPrompt 的策略截断）、专用 system 提示词（只修语法、逐字保留内容、
  输出完整全文无围栏）、不带历史不受 token 预算裁剪。
- `src/components/AiPanel.tsx`：
  - `send()` opts 增 `preset?: { messages; autoReview? }` —— preset 时跳过历史组装；
  - `autoReviewIdRef` 只在 **onDone** 写入 → effect 消费自动 openReview。出错/停止
    天然不触发（半截回复的 diff 会显示大片误删）；
  - 常驻「修复格式」按钮（.ai-fix-btn，不依赖 aiQuickActions，老用户免迁移）；
  - `AiPanelHandle` 增 `fixFormat()`，经 fixFormatRef 保持句柄空依赖稳定。
- `MenuBar.tsx` 格式菜单 + `App.tsx` dispatchMenu case `format_fix_md`（rAF 轮询等
  面板挂载，仿 onAskSelection）+ **Ctrl+Alt+F**（case "f" 最前加 altKey 分支）。
- `global.css` `.ai-fix-btn` accent 描边区分内置按钮。
- 版本号 4.4.0 → 4.5.0（package.json / tauri.conf.json / Cargo.toml / Cargo.lock）。

### 注意
- 工作区还有一批**未提交**的 bigDocPerformance（大文档性能模式）改动（types/
  memory/useSettings/useMilkdown/SettingsModal/performance.md），与本功能零重叠，
  提交时注意分开或一起发版。
- 写回路径复用现有 applyAiChanges → aiWriteDoc（一步撤销），未新写任何编辑器代码。
